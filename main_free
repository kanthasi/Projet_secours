for(int i = 0; i < N_SOMMETS; i ++)
    {
        for(int j = 0; j < N_SOMMETS; j ++)
        {
            if(graphe[i][j] != NULL)
            {
                free(graphe[i][j]);
                graphe[i][j] = NULL;
            }
        }
    }
